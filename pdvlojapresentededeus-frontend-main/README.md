# PDV Genérico - Frontend

Sistema de PDV e gerenciamento de estoque genérico (Next.js + React + Tailwind).

## Como rodar

1. `npm install`
2. Copie `.env.local.example` para `.env.local` e ajuste a URL da API se necessário
3. `npm run dev`
4. Acesse http://localhost:3000

## Login inicial (criado automaticamente pelo backend)

- **Admin**: login `admin`, senha `admin123`
- **Caixa**: login `caixa`, senha `caixa123`

> Troque essas senhas assim que possível pela tela de Usuários (admin).
